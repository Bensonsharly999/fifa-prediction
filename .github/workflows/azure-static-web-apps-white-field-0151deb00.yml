name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - Test
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - Test

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    permissions:
       id-token: write
       contents: read
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: false
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
      - name: Build application
        run: npm run build
      - name: Prepare for deployment
        run: |
          if [ -f "dist/fifa-bet-app/browser/index.csr.html" ]; then
            mv "dist/fifa-bet-app/browser/index.csr.html" "dist/fifa-bet-app/browser/index.html"
          fi
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_VICTORIOUS_BAY_0BD5D1100 }}
          action: "upload"
          app_location: "/"
          api_location: ""
          output_location: "dist/fifa-bet-app/browser"
          skip_deploy_on_missing_secrets: true

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          action: "close"
